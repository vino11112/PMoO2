<Window x:Class="WpfApp1.laborantiss"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="laborantiss" Height="450" Width="800">
    <Grid>
       <!-- <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       
        <ComboBox x:Name="cbAnalyzers" Grid.Row="0" Margin="10"
                  DisplayMemberPath="Name"
                  SelectionChanged="CbAnalyzers_SelectionChanged"/>

     
        <TabControl Grid.Row="1">
            <TabItem Header="Невыполненные исследования">
                <DataGrid x:Name="dgPendingServices" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Код" Binding="{Binding Service.Code}"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Service.Name}" Width="*"/>
                        <DataGridTextColumn Header="Тип результата" Binding="{Binding Service.ResultType}"/>
                        <DataGridTemplateColumn Header="Действия">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Отправить на исследование" 
                                            Click="BtnSendToAnalyzer_Click"
                                            IsEnabled="{Binding AnalyzerId, Converter={StaticResource NullToBoolConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Выполняющиеся исследования">
                <DataGrid x:Name="dgInProgressServices" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Код" Binding="{Binding Service.Code}"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Service.Name}" Width="*"/>
                        <DataGridTextColumn Header="Анализатор" Binding="{Binding Analyzer.Name}"/>
                        <DataGridTemplateColumn Header="Прогресс">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ProgressBar Value="{Binding Progress}" Maximum="100" Height="20"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Результат" Binding="{Binding Result}"/>
                        <DataGridTemplateColumn Header="Действия">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Одобрить" Margin="0,0,5,0"
                                                Click="BtnApproveResult_Click"
                                                IsEnabled="{Binding Result, Converter={StaticResource NullToBoolConverter}}"/>
                                        <Button Content="Отклонить" 
                                                Click="BtnRejectResult_Click"
                                                IsEnabled="{Binding Result, Converter={StaticResource NullToBoolConverter}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl> -->
    </Grid>
</Window>
